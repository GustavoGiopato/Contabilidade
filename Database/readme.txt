Esses são 3 arquivos de backup do exato mesmo banco. A unica diferença é o formato deles, 
para que se um der problema de compatibilidade, os outros funcionem corretamente.

Esse banco é usado para executar o código, instale no postgree e gerencie a conexão.